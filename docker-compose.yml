version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:80"
    depends_on:
      - db

  db:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: studentsdb
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: